---
name: humanizer
description: |-
  Rewrite AI-sounding text (Chinese or English) so it reads naturally without changing what it says. 中文场景触发词：去AI味、人味、AI腔、像人写的、改得自然。Use for any text the user asks to humanize, de-AI, or make sound more human. v5.0 architecture (rebuilt on Nanako0129/sepia's measured evidence): three-layer model — narrative architecture → discourse flow → surface style, deepest layer fixed first; four operations — write / review (diagnose only) / refactor (minimal edits) / recreate (full rewrite); calibration to the human band instead of inverting AI tells. Chinese coverage: six context registers (S1-S3 casual / W1-W3 formal) + simplified-Chinese calibration + full Traditional-Chinese Taiwan calibration; China programmatic documents (applications, self-statements) carry dedicated fingerprints (value-sublimation tables, cognition arcs, register drift).
---

# 去 AI 味改写规则（humanizer v5.0）

**底本演进**：v2-v4.6 基于 [blader/humanizer](https://github.com/blader/humanizer)（Wikipedia "Signs of AI writing" 35 条）+ 中文场景扩展，经 v3→v4.6 六轮实测修补。**v5.0（2026-09-19）按 [Nanako0129/sepia](https://github.com/Nanako0129/sepia)（MIT, v0.11.0）的实测证据骨架完全重构**，v4.6 的中文场景资产（六档语境、中国程式文书指纹、人民日报句库、0b 代写流程、九测反模板）全部保留并整合进新架构。

## 〇、总纲：三层模型与校准三原则

### 三层模型（为什么表层改写不够）

AI 味分三层，**修复顺序 deepest first**。StoryScope（61,608 篇故事实测）：仅凭**叙事架构层**特征检测 AI 文本 macro-F1 达 93.2%；人类编辑改写**表层风格**后，检测率仅从 95.5% 降到 93.9%——只改措辞几乎无效。

| 层 | 内容 | sepia 系文件 |
|---|---|---|
| 第 1 层 叙事架构 | 主题直给、因果单线、结局收束模式、时间线性、情绪呈现模式、角色网络密度、外部世界指涉 | `narrative-pass.md` |
| 第 2 层 篇章推进 | 段落问题序列模板（QUD）、中段塌陷、页面结构位置、开头、命名 | `discourse-pass.md` |
| 第 3 层 措辞风格 | 陈词滥调、句法模板、AI 高频词、句长节奏、语域 | `style-pass.md` + 语言校准文件 |

中文个人文书（W1-W3）的对应层见 `written-base.md` W0.6-W0.12（篇章级配额）与 `selfcheck.md` 九测——v4.x 实测出的骨架层/升华层指纹即第 1-2 层在中文文书场景的形态。

### 校准三原则（统治一切规则的规则）

1. **瞄准人类分布带，不反转 AI 分布**。人类指标落在中段（如时间非线性 2.4/5，不是 5/5）；把每条规则都用上，会形成一套新的「人味过载」指纹。专业文体的对应形式：匹配场合语域，不要矫枉过正成刻意随意——不正式本身骗不过训练过的读者。
2. **选择制，不叠加**。人类写作是多样的。小说每篇只选 3-5 种人类倾向手法（按题材选，跨作品轮换）；专业文档只修 checklist 实际命中的，仅此而已。
3. **留余地**。允许普通句子、没展开完的想法、平实的段落。不要把每一处都打磨出特色。这是语料库层面的规律（Sourati et al. 2026：模型重写 1000 篇人类文本后，文本间复杂度分布收窄 21-50%），单篇是否有过度打磨是阅读判断。

## 一、核心原则（改写时必须遵守）

1. **不改变原意**：保留原文的全部事实信息（名称、数字、日期、引文、结论），不增加、不删除、不夸大、不缩小。此约束限**事实信息层**；语言形式层的人味元素（错字、语气词、标点、瑕疵事件等）不受此限，按所选场景档的正向策略与配额执行。
2. **不编造事实**：不得添加原文没有的细节、例子、数据或引文。缺信息就把句子写简单些，而不是补内容。**查证通道（web search 优先）**：写作用到的事实（数据、人名、日期、引文、来源）缺失或没有把握时，先用 web search 自行检索求证——查到可靠来源保守补入（可溯源），查不到仍按本条处理（写简单些或删），绝不凭记忆编造。web search 不可用时直接按本条兜底。自信的错误事实本身是顶级 AI 痕迹。
3. **匹配原文语气与作者声音**：正式文本保持正式，口语文本保持口语；先从用户样本或场合近期真实文本提取习惯，朝**那个**画像改，不删除作者真实使用的口头禅（sepia：Respect the author's voice）。
4. **改结构不改信息**：可以合并/拆分段落、调整句序、缩短冗余，但信息量不损失。

0b 代写场景（无原文）的原则映射：原则 1/4 的「原文」替换为「任务要点清单」——清单之外的事实不得编造；原则 2 照常且为代写第一原则；原则 3 的语气基准为用户指定的目标场景档。任务点缺事实支撑时同样先走查证通道：查到保守补入，查不到改标「弃」并在交付说明言明。

## 二、路由（先路由，后操作）

| 文本类型 | 按序加载 |
|---|---|
| 小说/虚构故事/文学性叙事散文 | `narrative-pass.md` → `discourse-pass.md` → `style-pass.md`；诊断用 `rubric.md` |
| W3 正式应用文（申请书/自述/思想汇报/总结/报告） | `written-base.md` + `register-W3.md` → `discourse-pass.md` §1-3 → `style-pass.md`（跳过小说 slop 表） |
| W1 议论文/时评 | `written-base.md` + `register-W1.md`（按需 `renmin-examples.md`）→ `discourse-pass.md` §1-3 → `style-pass.md` |
| W2 说明性文章 | `written-base.md` + `register-W2.md` → `discourse-pass.md` §1-3 → `style-pass.md` |
| S1/S2/S3 口语档 | `colloquial-base.md` + `register-Sx.md` → `style-pass.md` §5（节奏）+ 语言校准（风格层被 S 档正向策略覆盖，见调和规则） |
| 发版说明/changelog/公告 | `professional-pass.md` + `domains/release-notes.md` |
| PR/issue 回复、评审意见 | `professional-pass.md` + `domains/dev-replies.md` |
| 事故复盘/RCA | `professional-pass.md` + `domains/postmortems.md` |
| 工单/工单报告/bug 报告 | `professional-pass.md` + `domains/tickets.md` |
| 技术文章/博客/教程 | `professional-pass.md` + `domains/tech-articles.md` + `discourse-pass.md` §1-3 |
| 长篇新闻（特稿/调查/数据报道/采访） | `professional-pass.md` + `domains/journalism.md` + `discourse-pass.md` §1-3 |
| 其他散文 | `professional-pass.md` + `style-pass.md` |

- **语言校准**：目标文本为中文时，在 style-pass 步骤**加载** `zh-hans.md`（简体，默认）或 `zh-hant.md`（繁体台湾场合——台湾机构发文/繁体读者场合；简体文本即使发台湾场合也不用它）。英文文本不加载。
- **模型指纹**（可选进阶）：已知起草或执行模型时，按 `model-fingerprints.md` 的模型身份规则加载对应家族的指纹层（叙事层仅小说向；散文层所有路径 style-pass 步）。判不出就 `none`，**禁止从文风反推模型**。
- 每条非虚构路由以 `style-pass.md` §2-3 词汇句法扫描与 §5 节奏检查收尾；refactor 另跑 §4 末段的删除/回退测试；长篇专业文本跑整个 style-pass（跳过小说 slop 表）。

### 场景档判定（中文日常/写作场景前置步骤）

判定依据优先级：用户明示场景 > 文本形态特征（长度、标点密度、平台词汇、格式）。

| 档位 | 子类 | 典型场景 |
|---|---|---|
| 口语 | S1 熟人闲聊 | 朋友私聊、群聊 |
| 口语 | S2 社区公开发言 | 贴吧/知乎/B站/微博/豆瓣的评论与回复 |
| 口语 | S3 半正式交流 | 向老师/上级汇报、学习群、工单、求助帖 |
| 书面 | W1 议论文/时评 | 政论、考场/竞赛议论文 |
| 书面 | W2 说明性文章 | 科普、解读、综述 |
| 书面 | W3 正式应用文 | 申请书、总结、报告 |

**加载纪律**：动笔前必须读完所选档位必读文件（加载表见路由表）；文件读取失败时降级：只按本文件通用规则 + 调和总原则做保守书面化处理，并在交付说明中标注「档位文件缺失」。判定不出时问用户一句「这段文字要用在什么场合」；无法询问时口语侧按 S3、书面侧按 W1；大档亦无法判定时默认 S3（最保守档）并注明存疑。

### 调和规则（底线与场景档正向策略冲突时）

- §格式规范化类规则（加粗还原/emoji 删除/标点规范化）仅在书面语境全面生效；口语语境下转为「配额制人味元素」，按 S1-S3 各自配额。
- 破折号：中文书面正式文书零破折号（`zh-hans.md` 输出规范）；口语档按 S0.4 标点策略。
- 填充短语压缩在口语档降级为方向性偏好（S0.5 瑕疵事件授权）；「对…进行+动词」类功能性虚化在书面档仍一律压缩。
- 口语档省略主语按 S0.8 授权执行；「补出主语」仅在需要说清动作执行者时适用。
- 「说实话/讲真」：口语档句中插入语/话头垫语可用；作开场白包装观点仍禁。
- 限定词：回加单个语气标记（人类语域恢复）与压缩堆叠限定词（可能或许/某种意义上或许可以说）不冲突——回加一个、压堆叠。
- 调和总原则：凡规范化/去冗余/补全方向的改法与 S0-S3 正向策略冲突时，口语档以正向策略为准，但仅限该档明文授权的人味元素且计入配额；拿不准按书面化处理。W3 应用文仍走规范，勿把正式文写坏。

## 三、四操作（任何请求映射到其一）

| 操作 | 契约 |
|---|---|
| **write**（撰写新内容，含 0b 代写） | 动笔**前**读领域/档位文件——架构与语域决策不能事后补救。0b 流程：任务点列清单→逐点标注处置（合并/错位/加重/轻放/弃；加重轻放各 ≤1 处，其余点用合并/错位/弃）→按情绪鲜活度排段序、禁逐点成段→大纲定稿前对目标文体模板链做一次结构变化（written-base W0.11），并把所选结构变化与「保护资产清单」（人→动作→场景具体链，最不像 AI 的部分）一并写进标注表留档→列材料清单逐条过核验（zh-hans 引用纪律）并把清单留档备查→按选定档位正向策略成文 |
| **review**（仅诊断，不改） | 产出缺陷清单即止：小说/叙事用 `rubric.md` 分组报告；专业/文书用对应 checklist findings + 引文证据。未获要求不应用任何修复 |
| **refactor**（最小原地修改） | **两阶段**：先出全量缺陷清单，再逐项修，最深层优先。偏向替换/删除而非新增（实测编辑比例 74/18/8）。新增词跑**删除测试**（删掉后句意句法仍成立即删）；替换处跑**回退测试**（换回原词更省字且同义即回退）；修复不算增长（断裂句需要的连接词/主语/动词是修复）。改写不得比原文更营销腔 |
| **recreate**（整篇重写） | 从原文提取事实/主张/意图为裸清单→核实无编造→按领域规则重写。适用于缺陷属结构层且文本短到手术不如重建 |

两阶段协议对 refactor/recreate 不可选：无缺陷清单的逐句改写会让 AI 指纹**更**明显（专家检测器实测）。

**交付格式**：write 与 recreate 为初稿 + 残留 AI 模式清单 + 终稿三段交付；refactor 缺省两段（终稿 + 残留清单），结构调整幅度大时按三段；收信人可能鉴别作者身份的场景（turing-test）按自然度优先原则处置漏点；红队自评：从收信人视角估 AI 概率，超 40% 修订一次再交付（详见 `selfcheck.md`）。

## 四、硬护栏（所有操作无条件生效）

1. **绝不编造具体细节**。虚构作品中互文引用/品牌/地名必须真实正确；专业文档的版本/数字/时间戳/基准/引文来自真实变更/事故/数据——缺信息就问用户或留显式 TODO，绝不补。
2. **删除优于新增**（74% 替换 / 18% 删除 / 8% 新增，LAMP 实测）。允许的新增仅限：真实具体性、断裂句修复所需成分、删除填充后的等量回补；段落不得比原来长（修复不是增长）。
3. **尊重作者声音与场合语料**。动笔前采样场合近期真实文本（仓库过往发版说明/维护者近期回复/团队上次复盘），匹配其语域、长度习惯、格式习惯；场合语料缺席时用领域文件基线。
4. **对话引文/引用材料/保护区间是承重的**——不得规范化。改写中引文内文字永不改动；发现其中缺陷照报不修。
5. **无人值守模式**：无法停下来问用户的场景，需要用户决策的缺陷记录在 `Deferred:` 行并保持原样。「沉默」与「跳过的缺陷」是两个事实，报告分开表述。
6. **先查白名单再定罪**：干净语法、正式场合的正式语气、常规模板不是 AI 证据（`style-pass.md` §7 / `professional-pass.md` 白名单 / `zh-hans.md` 白名单）。单个命中不是判决——**slop 是累积的**，聚簇才动手。
7. **核验债显式化**：引用材料分三级状态（已验证/待核验/已知库错）；未当场查证的材料按方向性陈述写，不写成精确归因（详见 `zh-hans.md` 引用纪律与 `written-base.md` W0.5）。

## 五、诊断协议（review 与 refactor 阶段 1）

1. **一次只读一组**（rubric 分 A-E 组 + F 组中国文书指纹）：模型自评全文会坍缩到一两个显眼维度（实测 span precision 0.13-0.16），必须分次读。
2. **无引文不算信号**：每个观察到的信号必须引用原文短句作证。
3. **n/a 显式标注**：文本无判断场合的特征标 n/a，不强行判断；短文本多个 n/a 是预期行为。
4. **过度修正独立标注**：数值落在远离 AI 方向的极端（如时间非线性 5/5）标「过度修正警告」，这是人味过载指纹，不计入 AI 倾向信号。

## 六、改写流程（refactor/recreate 遵循）

1. **路由**：判文本类型 → 判场景档（中文场景）→ 判语言 → 按第二节路由表加载全部必读文件。
2. **诊断**（refactor 阶段 1 / review）：按第五节协议分组读，产出带引文的全量缺陷清单；中文文书场景叠加 `selfcheck.md` 九测对应维度与 `written-base.md` W0 配额检查。
3. **修复**（refactor 阶段 2）：最深层优先修（叙事/篇章结构 → 骨架/升华 → 句法/词汇）；每处修复对照缺陷清单编号；受场景档配额约束的人味元素按档位文件执行。
4. **自检**：跑 `selfcheck.md` 反模板清单测（①-⑨按分流表）+ 所选档位验收清单（colloquial-base/written-base 末节）+ 删除/回退测试。
5. **交付**：按第三节交付格式输出；无人值守场景补 `Deferred:`/`Protected:` 行。

**快速清单模式**（外部审稿场景引用本技能时）：不需全流程，直接加载 `zh-hans.md`（中文）或 `style-pass.md` §2-3（英文）对照清单逐段核查，报告命中项+引文即可——这是各类外部审稿流程的接入点。

## 七、版本摘要

- **v5.0（2026-09-19 sepia 骨架重构）**：架构从「35 条底线清单 + 六档路由 + 九测」重构为「三层模型 + 双维路由 + 四操作契约 + 分组诊断协议 + 校准三原则」；sepia 三 pass/rubric/professional/六领域/模型指纹全量编译入库（references/）；新增简体校准 `zh-hans.md` 与繁体台湾校准 `zh-hant.md`；35 条底线逐条安置进新文件（安置映射表存开发工作区档案）；九测反模板独立为 `selfcheck.md`；v4.6 全部中国场景资产（六档/W0.1-W0.12/人民日报句库/0b 流程/查证通道/保护资产）保留并接入新路由。
- v4.x 摘要（v3.2 逻辑调和 / v4.1 查证通道 / v4.2 引用核验 / v4.3 篇章指纹 / v4.4 骨架指纹 / v4.5 升华指纹 / v4.6 认识弧与语域漂移）的规则本体全部仍在库内：`written-base.md` W0.1-W0.12、`register-W3.md`、`colloquial-base.md` S0.1-S0.9、`selfcheck.md`。
